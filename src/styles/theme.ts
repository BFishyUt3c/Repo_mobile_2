// Paleta de colores inspirada en el frontend web y branding Greenloop
export const colors = {
  primary: '#388e3c', // Verde principal
  primaryDark: '#256029',
  primaryLight: '#e8f5e9',
  accent: '#ff9800', // Naranja/acento
  error: '#d32f2f',
  warning: '#ffb300',
  info: '#1976d2',
  success: '#4caf50',
  background: '#f5f5f5',
  white: '#fff',
  black: '#222',
  gray: '#888',
  lightGray: '#eee',
};

// Tipografía base
export const fonts = {
  regular: 'System',
  bold: 'System',
  // Puedes agregar fuentes personalizadas si las usas en Expo
};

// Tamaños de fuente
export const fontSizes = {
  title: 24,
  subtitle: 18,
  body: 15,
  small: 12,
};

// Espaciados
export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
};

// Bordes
export const borderRadius = {
  sm: 6,
  md: 12,
  lg: 24,
};

// Sombra (para tarjetas)
export const shadow = {
  shadowColor: colors.black,
  shadowOffset: { width: 0, height: 2 },
  shadowOpacity: 0.1,
  shadowRadius: 4,
  elevation: 3,
}; 