import React from 'react';
import { View, Text } from 'react-native';

const AIScreen: React.FC = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>AI/Asistente (en construcción)</Text>
    </View>
  );
};

export default AIScreen; 